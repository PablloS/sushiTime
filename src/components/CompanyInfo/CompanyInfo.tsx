import { CompanyInfoProps } from "./CompanyInfo.props";
import styles from "./CompanyInfo.module.css"; 


const CompanyInfo = ({}: CompanyInfoProps): JSX.Element => {
    return (
        <div className={styles.wrapper}>

        </div>
    )
}

export default CompanyInfo; 